% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/balnet.R
\name{print.balnet}
\alias{print.balnet}
\title{Print a balnet object.}
\usage{
\method{print}{balnet}(x, digits = max(3L, getOption("digits") - 3L), max = 3, ...)
}
\arguments{
\item{x}{A \code{balnet} object.}

\item{digits}{Number of digits to print.}

\item{max}{Total number of rows to show from the beginning and end of the path}

\item{...}{Additional print arguments.}
}
\value{
Invisibly returns a data.frame with the printed information.
}
\description{
Print a balnet object.
}
\examples{
\donttest{
n <- 100
p <- 25
X <- matrix(rnorm(n * p), n, p)
W <- rbinom(n, 1, 1 / (1 + exp(1 - X[, 1])))

# Fit an ATE model.
fit <- balnet(X, W)

# Print path summary.
print(fit)

# Plot path diagnostics.
plot(fit)

# Plot covariate imbalance at the end of the path (closest to lambda = 0).
plot(fit, lambda = 0)

# Predict propensity scores.
pp <- predict(fit, X)

# Extract coefficients.
coefs <- coef(fit)
}

}
