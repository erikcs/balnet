% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/balnet.R
\name{plot.balnet}
\alias{plot.balnet}
\title{Plot diagnostics for a \code{balnet} object.}
\usage{
\method{plot}{balnet}(x, lambda = NULL, max = NULL, ...)
}
\arguments{
\item{x}{A \code{balnet} object.}

\item{lambda}{If NULL (default) diagnostics over the lambda path is shown.
Otherwise, diagnostics for a single lambda value is shown.
(if target = "ATE", lambda can be a 2-vector, arm 0 and arm 1.)}

\item{max}{The number of covariates to display in balance plot. Defaults to all covariates.}

\item{...}{Additional arguments.}
}
\value{
Invisibly returns a list with the information underlying the plot.
}
\description{
Plot diagnostics for a \code{balnet} object.
}
\examples{
\donttest{
n <- 100
p <- 25
X <- matrix(rnorm(n * p), n, p)
W <- rbinom(n, 1, 1 / (1 + exp(1 - X[, 1])))

# Fit an ATE model.
fit <- balnet(X, W)

# Print path summary.
print(fit)

# Plot path diagnostics.
plot(fit)

# Plot covariate imbalance at the end of the path (closest to lambda = 0).
plot(fit, lambda = 0)

# Predict propensity scores.
pp <- predict(fit, X)

# Extract coefficients.
coefs <- coef(fit)
}

}
