% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cv.balnet.R
\name{predict.cv.balnet}
\alias{predict.cv.balnet}
\title{Predict using a cv.balnet object.}
\usage{
\method{predict}{cv.balnet}(object, newx, lambda = "lambda.min", type = c("response"), ...)
}
\arguments{
\item{object}{A \code{cv.balnet} object.}

\item{newx}{A numeric matrix.}

\item{lambda}{The lambda to use. Defaults to the cross-validated lambda.}

\item{type}{The type of predictions.}

\item{...}{Additional arguments (currently ignored).}
}
\value{
Predictions.
}
\description{
Predict using a cv.balnet object.
}
\examples{
\donttest{
n <- 100
p <- 25
X <- matrix(rnorm(n * p), n, p)
W <- rbinom(n, 1, 1 / (1 + exp(1 - X[, 1])))

# Fit an ATE model.
cv.fit <- cv.balnet(X, W)

# Predict at cross-validated lambda.
pp <- predict(cv.fit, X)
}

}
