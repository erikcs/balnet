% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/balnet.R, R/cv.balnet.R
\name{balweights}
\alias{balweights}
\alias{balweights.balnet}
\alias{balweights.cv.balnet}
\title{Extract balancing weights from a balnet object.}
\usage{
balweights(object, lambda = NULL, ...)

\method{balweights}{balnet}(object, lambda = NULL, ...)

\method{balweights}{cv.balnet}(object, lambda = "lambda.min", ...)
}
\arguments{
\item{object}{A \code{balnet} object.}

\item{lambda}{Value(s) of the penalty parameter \code{lambda} at which weights
are required.
\itemize{
\item If \code{NULL} (default), the full lambda path from the fit is used.
\item If new values are supplied, linear interpolation is performed.
For dual-arm fits (\code{target} = "ATE"), \code{lambda} can be a \code{list} or
two-column \code{matrix}: the first element/column corresponds to the control
arm and the second to the treatment.
}}

\item{...}{Additional arguments (currently ignored).}
}
\value{
Estimated balancing weights
(for contrast fits, \code{target} = "ATE" or "ATT", returns a list with entries for each arm).
}
\description{
Retrieves the estimated inverse probability weights \eqn{\hat{\gamma}}.
}
\examples{
\donttest{
n <- 100
p <- 25
X <- matrix(rnorm(n * p), n, p)
W <- rbinom(n, 1, 1 / (1 + exp(1 - X[, 1])))

# Fit an ATT model.
fit <- balnet(X, W, target = "ATT")

# Extract balancing weights.
wts <- balweights(fit, lambda = 0)
}

}
