% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cv.balnet.R
\name{coef.cv.balnet}
\alias{coef.cv.balnet}
\title{Extract coefficients from a cv.balnet object.}
\usage{
\method{coef}{cv.balnet}(object, lambda = "lambda.min", ...)
}
\arguments{
\item{object}{A \code{cv.balnet} object.}

\item{lambda}{The lambda to use. Defaults to the cross-validated lambda.}

\item{...}{Additional arguments (currently ignored).}
}
\value{
Estimated logistic coefficients
(for dual-arm fits, returns a list with entries for each arm).
}
\description{
Extract coefficients from a cv.balnet object.
}
\examples{
\donttest{
n <- 100
p <- 25
X <- matrix(rnorm(n * p), n, p)
W <- rbinom(n, 1, 1 / (1 + exp(1 - X[, 1])))

# Fit an ATT model.
cv.fit <- cv.balnet(X, W, target = "ATT")

# Extract coefficients at cross-validated lambda.
coefs <- coef(cv.fit)
}

}
