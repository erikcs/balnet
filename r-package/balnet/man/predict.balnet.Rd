% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/balnet.R
\name{predict.balnet}
\alias{predict.balnet}
\title{Predict using a balnet object.}
\usage{
\method{predict}{balnet}(object, newx, lambda = NULL, type = c("response"), drop = TRUE, ...)
}
\arguments{
\item{object}{A \code{balnet} object.}

\item{newx}{A numeric matrix.}

\item{lambda}{Value(s) of the penalty parameter \code{lambda} at which coefficients
are required.
\itemize{
\item If \code{NULL} (default), the full lambda path from the fit is used
(if new values are supplied, linear interpolation is performed).
\item For dual-arm fits (control and treatment), \code{lambda} can be a \code{list} or
two-column \code{matrix}: the first element/column corresponds to the control
arm and the second to the treatment arm.
}}

\item{type}{The type of predictions.}

\item{drop}{Whether to drop the arm naming for single-arm fits. Default is TRUE.}

\item{...}{Additional arguments (currently ignored).}
}
\value{
Predictions.
}
\description{
Predict using a balnet object.
}
\examples{
\donttest{
n <- 100
p <- 25
X <- matrix(rnorm(n * p), n, p)
W <- rbinom(n, 1, 1 / (1 + exp(1 - X[, 1])))

# Fit an ATE model.
fit <- balnet(X, W)

# Predict propensity scores.
pp <- predict(fit, X)
}

}
