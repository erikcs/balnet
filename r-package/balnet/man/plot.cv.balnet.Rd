% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cv.balnet.R
\name{plot.cv.balnet}
\alias{plot.cv.balnet}
\title{Plot diagnostics for a \code{cv.balnet} object.}
\usage{
\method{plot}{cv.balnet}(x, lambda = "lambda.min", ...)
}
\arguments{
\item{x}{A \code{cv.balnet} object.}

\item{lambda}{The lambda to use. Defaults to the cross-validated lambda.}

\item{...}{Additional arguments.}
}
\description{
Plot diagnostics for a \code{cv.balnet} object.
}
\examples{
\donttest{
n <- 100
p <- 25
X <- matrix(rnorm(n * p), n, p)
W <- rbinom(n, 1, 1 / (1 + exp(1 - X[, 1])))

# Fit an ATE model.
cv.fit <- cv.balnet(X, W)

# Plot at cross-validated lambda.
plot(cv.fit)
}

}
