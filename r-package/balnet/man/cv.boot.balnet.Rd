% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cv.boot.balnet.R
\name{cv.boot.balnet}
\alias{cv.boot.balnet}
\title{Tuning via subsampling for balnet.}
\usage{
cv.boot.balnet(X, W, B = 500, ...)
}
\arguments{
\item{X}{A numeric matrix or data frame with pre-treatment covariates.}

\item{W}{Treatment vector (0: control, 1: treated).}

\item{B}{The number of bootstrap replicates.}

\item{...}{Arguments for \code{\link{balnet}}.}
}
\value{
A fit cv.boot.balnet object (same functionality as cv.balnet)
}
\description{
Tunes a balnet object using sub sampled imbalances calculated using full-sample weights
a la Wang & Zubizarreta (2020).
}
\examples{
\donttest{
n <- 100
p <- 25
X <- matrix(rnorm(n * p), n, p)
W <- rbinom(n, 1, 1 / (1 + exp(1 - X[, 1])))

# Fit an ATE model.
cv.fit <- cv.boot.balnet(X, W)

# Print CV summary.
print(cv.fit)

# Plot at tuned lambda.
plot(cv.fit)

# Predict at tuned lambda.
W.hat <- predict(cv.fit, X)

}

}
\references{
Wang, Yixin, and Jose R. Zubizarreta.
"Minimal dispersion approximately balancing weights: asymptotic properties and practical considerations."
Biometrika 107(1), 2020.
}
