% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/balnet.R, R/cv.balnet.R
\name{ipw}
\alias{ipw}
\alias{ipw.balnet}
\alias{ipw.cv.balnet}
\title{Extract IPW weights from a balnet object.}
\usage{
ipw(object, lambda = NULL, ...)

\method{ipw}{balnet}(object, lambda = NULL, ...)

\method{ipw}{cv.balnet}(object, lambda = "lambda.min", ...)
}
\arguments{
\item{object}{A \code{balnet} object.}

\item{lambda}{Value(s) of the penalty parameter \code{lambda} at which weights
are required.
\itemize{
\item If \code{NULL} (default), the full lambda path from the fit is used
(if new values are supplied, linear interpolation is performed).
\item For dual-arm fits (control and treatment), \code{lambda} can be a \code{list} or
two-column \code{matrix}: the first element/column corresponds to the control
arm and the second to the treatment arm.
}}

\item{...}{Additional arguments (currently ignored).}
}
\value{
Estimated IPW weights. For contrast fits (ATE and ATT),
returns a list containing IPW weights for each arm.
}
\description{
Extract IPW weights from a balnet object.
}
\examples{
\donttest{
n <- 100
p <- 25
X <- matrix(rnorm(n * p), n, p)
W <- rbinom(n, 1, 1 / (1 + exp(1 - X[, 1])))

# Fit an ATE model.
fit <- balnet(X, W)

# Extract IPW weights.
wts <- ipw(fit, lambda = 0)
}

}
