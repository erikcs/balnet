% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cv.balnet.R
\name{print.cv.balnet}
\alias{print.cv.balnet}
\title{Print a cv.balnet object.}
\usage{
\method{print}{cv.balnet}(x, digits = max(3L, getOption("digits") - 3L), ...)
}
\arguments{
\item{x}{A \code{cv.balnet} object.}

\item{digits}{Number of digits to print.}

\item{...}{Additional print arguments.}
}
\description{
Print a cv.balnet object.
}
\examples{
\donttest{
n <- 100
p <- 25
X <- matrix(rnorm(n * p), n, p)
W <- rbinom(n, 1, 1 / (1 + exp(1 - X[, 1])))

# Fit an ATE model.
cv.fit <- cv.balnet(X, W)

# Print CV summary.
print(cv.fit)

# Plot at cross-validated lambda.
plot(cv.fit)

# Predict at cross-validated lambda.
pp <- predict(cv.fit, X)

# Extract coefficients at cross-validated lambda.
coefs <- coef(cv.fit)
}

}
